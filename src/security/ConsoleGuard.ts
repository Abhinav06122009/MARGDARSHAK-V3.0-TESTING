/**
 * ConsoleGuard.ts
 * Prevents Self-XSS attacks by warning users about the dangers of pasting code into the console.
 */

export const initConsoleGuard = () => {
  // Console Guard Disabled for debugging phase
  return;
};
